General:

- Add categories on posts index view.
- Fix posts index table size.
- Add current language on posts index title.
- Fix error on relay media field selector.

Interface:

- Add new queries interface.
- Add blog__get_posts to get posts objects.
- Add blog__get_post to get a single post object.
- Add blog__get_categories to get categories objects.
- Add blog__get_category to get a single category object.