<%
  row = cell(:elements, :row).new
  block = cell(:elements, :block).new(class: 'xs-12 sm-12 md-12')
%>

<!-- MAIN CONTAINER -->
<%=raw row.open %>

  <%=raw block.open %>

    <%=raw cell(:widgets, :index).new(records: @widget_index_categories, index_url: lato_blog.categories_path,
    head: [
      LANGUAGES[:lato_blog][:forms][:id],
      LANGUAGES[:lato_blog][:forms][:title],
    ], columns: ['id', "title"],
    actions: {show: true, edit: true, delete: true, new: true}, pagination: true) %>

  <%=raw block.close %>

<%=raw row.close %>
<!-- / MAIN CONTAINER -->