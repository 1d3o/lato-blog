<%
  row = cell(:elements, :row).new
  block = cell(:elements, :block).new(class: 'xs-12 sm-12 md-12')
%>

<!-- MAIN CONTAINER -->
<%=raw row.open %>

  <%=raw block.open %>

      <%=raw cell(:widgets, :index).new(records: @widget_index_categories, index_url: lato_blog.categories_path,
      head: [LANGUAGES[:lato_blog][:forms][:title], LANGUAGES[:lato_blog][:forms][:language]],
      columns: ['title', 'get_pretty_language'], actions: {show: true, new: true, delete: true}, search: true, pagination: true) %>

  <%=raw block.close %>

<%=raw row.close %>
<!-- / MAIN CONTAINER -->