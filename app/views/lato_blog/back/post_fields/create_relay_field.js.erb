<% if @error %>

  alert('There was an error')

<% else %>

  $('.fields-relay[data-field-id="<%= @post_field.id %>"]').replaceWith('<%=j render_post_field(@post_field) %>')
  
  // TODO: Aggiornare solo i field interni al ripetitore
  BlogFields.init()
  CoreDatepicker.init()
  CoreEditor.init()
  Mediapicker.init()

<% end %>